# SentinelLoop — minimal, strong stack
PyQt6>=6.6
requests>=2.31
python-dotenv>=1.0

# Optional: live LLM planner (demo mode works without it)
anthropic>=0.40

# Optional: route Splunk access through the Splunk MCP Server (set SPLUNK_MCP_URL)
mcp>=1.2
